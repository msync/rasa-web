(ns rasa.edn)

(defn read-string
  "Reads one EDN value from a string into Rasa data. Empty input returns nil."
  [s]
  (rasa.edn.impl/read-string s))

(defn write-string
  "Writes supported Rasa data as a compact EDN string."
  [value]
  (rasa.edn.impl/write-string value))
